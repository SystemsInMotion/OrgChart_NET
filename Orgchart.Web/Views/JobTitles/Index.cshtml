@model IEnumerable<Orgchart.Web.Models.JobTitle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descriprtion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descriprtion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobTitleId }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobTitleId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobTitleId })
        </td>
    </tr>
}

</table>
